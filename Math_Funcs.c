#include <stdio.h>
#include <math.h> //library for math functions

int main() {
    float a = sqrt(9); //for square root (output 3)
    float b = pow(2,4); //for power (output 64)
    int c = round(3.14); //for round off (output 3)
    int d = ceil(3.02); //for round up (output 4)
    int e = floor(3.9); //for round down (output 3)
    float f = fabs(-900); //for absolute value (output 900)
    float g = log(3); //for log values
    float h = sin(45); //for sin values, exists for other trigo funcs also

    //printf
}